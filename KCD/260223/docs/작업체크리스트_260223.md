# 260223 작업 체크리스트 — 보강 실험 수행 순서

**목적**: 4단계 파이프라인에 따라 어떤 과정·작업을 해야 하는지 구체적으로 정리.  
**참고**: `보강실험_로드맵_260223.md`와 함께 사용.

---

## Step 1: Data Preprocessing (데이터 전처리)

| # | 할 일 | 상세 | 참고 기존 코드 | 완료 |
|---|--------|------|----------------|------|
| 1-1 | 마스터 테이블 정의 | `df_base_features`에 포함할 컬럼 목록 정의 (업장 ID, growth_rate, new_customer_ratio, cv_sales_card, business_age_months, 지역·업종 등) | 260121 regenerate_store_features, 260204 run_01 | [ ] |
| 1-2 | 변수 매핑 확인 | 원본/파생 데이터에서 `new_customer_ratio`, `cv_sales_card`, `business_density`, `business_square_size` 등 로드맵 변수와 대응되는 컬럼명 확인 | basic_data/store_features_for_analysis.csv, variable_description.csv | [ ] |
| 1-3 | Step 1 스크립트/실행 경로 확정 | 260223에서 Step 1 전용 스크립트를 둘지, 260121/260204 출력을 그대로 `df_base_features`로 쓰도록 래퍼만 둘지 결정 | 260204 configs/base.yaml | [ ] |
| 1-4 | 산출물 생성 | `outputs/tables/df_base_features.csv` (또는 .parquet) 생성 | — | [x] |

---

## Step 2: Signal Extraction (시계열 특성 추출)

| # | 할 일 | 상세 | 참고 기존 코드 | 완료 |
|---|--------|------|----------------|------|
| 2-1 | K-Shape 라벨 확보 | K=6 클러스터 라벨 (260204 store_cluster_labels_K6.parquet) 사용 | 260204 run_02_cluster_fix | [ ] |
| 2-2 | 변곡점·U/D 라벨 확보 | P1/P2, slope, inflection_week (260204_gem inflection_p1p2_labels.csv) | 260204_gem run_01_inflection_p1p2 | [ ] |
| 2-3 | UDX 코드 확보 | U/D + X/Y/Z 결합 코드 (260204_gem final_code_by_store_260121_outlier_removal.csv) | 260204_gem step3_mapping_analysis | [ ] |
| 2-4 | df_udx_labels 통합 | 위 세 결과를 public_id 기준으로 merge → `outputs/tables/df_udx_labels.csv` | — | [x] |

---

## Step 3: Econometric Analysis (계량경제학적 분석) ★ 신규

| # | 할 일 | 상세 | 완료 |
|---|--------|------|------|
| 3-1 | 회귀용 데이터 준비 | df_base_features + df_udx_labels merge; 종속·독립·통제·더미 변수 리스트 확정 | [ ] |
| 3-2 | Logit (성장/쇠퇴 이진) | Y=성장형(1)/쇠퇴형(0). Model 1(기본 X) / Model 2(+통제) / Model 3(+sigungu, depth_2 고정효과). 계수·표준오차·p-value 저장 | [x] |
| 3-3 | OLS (growth_rate 연속) | 동일 Model 1/2/3 구조로 OLS 실행. 논문용 표 포맷(regression_tables.csv 또는 stargazer 등) 출력 | [x] |
| 3-4 | Event Study 데이터 구축 | 변곡점 시점(inflection_week)을 t=0으로 하여 업장별 주별 시계열 정렬; 전후 w주 구간 정의 | [x] |
| 3-5 | Event Study 시각화 | t=0 전후로 new_customer_ratio 또는 avg_sales 평균 추이 그래프 (event_study_plots.png) | [x] |
| 3-6 | 논문용 표 작성 | Model 1/2/3을 한 표에 나란히 배치한 결과 파일 저장 | [x] |

---

## Step 4: Predictive Modeling (예측 + SHAP) ★ 보강

| # | 할 일 | 상세 | 완료 |
|---|--------|------|------|
| 4-1 | 학습 데이터 구성 | 30주(또는 지정 윈도우) 피처 + UDX·변곡점 피처(P1 slope 등); 타깃=성장/하락 또는 연속값 | [ ] |
| 4-2 | Baseline vs ML | Baseline(이동평균/단순 규칙) vs Random Forest, XGBoost, LightGBM 성능 비교 | [x] |
| 4-3 | Ablation | (1) 기본 피처만 (2) 기본 + UDX + 변곡점 피처 — RMSE, F1 등 기록 (prediction_metrics.csv) | [x] |
| 4-4 | SHAP 계산 | 선택 모델(XGBoost 등)에 대해 SHAP Value 계산 | [ ] (shap 설치 시 가능) |
| 4-5 | SHAP 시각화 | shap_summary_plot.png 등으로 "성장 예측 시 어떤 변수가 기여했는지" 출력 | [x] (대안: feature_importance_rf.png) |

---

## 공통·인프라

| # | 할 일 | 완료 |
|---|--------|------|
| C-1 | 260223 configs 작성 | 데이터 경로, 변수명, 출력 경로 등 YAML/설정 파일 | [ ] |
| C-2 | 실행 순서 문서화 | 01 → 02 → 03 → 04 순서로 실행하는 방법을 README에 명시 | [ ] |
| C-3 | 변수명 매핑표 | 로드맵에서 쓰는 이름(new_customer_ratio 등) ↔ 실제 컬럼명 매핑표 (docs 또는 configs) | [ ] |

---

## 요약: 우선순위

1. **Step 3 (계량경제)**  
   - 회귀 Model 1/2/3 (Logit + OLS) + 고정효과  
   - Event Study (변곡점 전후 시계열 그래프)  
   → 논문 4장 "원인 규명" 채우기.

2. **Step 4 (예측·SHAP)**  
   - ML 모델 + Ablation (기본 vs 기본+UDX+변곡점)  
   - SHAP 시각화  
   → 논문 5장 "예측 및 해석" 보강.

3. **Step 1·2**  
   - 기존 260121/260204/260204_gem 산출물을 `df_base_features`, `df_udx_labels` 규격으로 정리하거나, 260223 전용 스크립트에서 동일 사양 생성.

위 체크리스트를 진행하면서 `보강실험_로드맵_260223.md`의 3가지 핵심 실험(① 정통 회귀, ② 사건연구, ③ ML+SHAP)이 모두 산출물로 이어지도록 하면 된다.
