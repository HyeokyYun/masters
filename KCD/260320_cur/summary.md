# 260320_cur 분석 요약

## 배경

2026-03-20 개별연구 미팅에서 `260301` 다항 로짓 결과에 대해 다음이 논의되었다.

1. **매출 변동성(`cv_sales_card`)**: 성장 추세가 있는 시계열에서 단순 평균 대비 표준편차는 해석이 왜곡될 수 있으므로, **추세(선형 또는 곡선)에 대한 잔차** 기준으로 변동성을 재정의할 것.
2. **업종 효과**: 패스트푸드·카페 등은 **동 단위 동 업종 밀도(경쟁)** 와의 **상호작용**을 넣어 볼 것.
3. **프랜차이즈**: 데이터가 불완전하므로 사용을 보류.
4. **대표자 연령·성별**: `meta`의 `age`는 수치화 시도. 성별은 현재 스키마에 없음.
5. **업력**: 짧은 업력만 서브샘플로 재추정, 분포 히스토그램 작성.
6. **조기예측 F1 ~0.37**: 소규모 매출/수요 예측 문헌에서 **비교 가능한 기준**을 별도 조사.

## 본 폴더에서 구현한 것

| 스크립트 | 내용 |
|----------|------|
| `run_01_trend_residual_cv.py` | 주간 `sales_card`에 선형·2차 추세 적합 후 잔차 기반 CV |
| `run_02_build_extended_dataset.py` | 동 단위 업종 밀도, meta 연령, 잔차 CV 병합 → `df_extended_for_regression.csv` |
| `run_03_extended_mnlogit.py` | 기존 변수 + (가능 시) 잔차 CV + 업종×밀도 상호작용 + 연령, MNLogit 3기준 |
| `run_04_subsample_by_age.py` | 업력 상한별 서브샘플 MNLogit (기준 Stable) |
| `run_05_business_age_histogram.py` | 업력 요약 통계 및 히스토그램 |

## 선행 데이터

- `260301/outputs/tables/df_for_multinomial_logit.csv` (또는 `260225` 폴백)
- `original_data/weekly.parquet`, `meta.csv`

## 해석 시 유의

- `run_01`은 전체 weekly를 읽어 **실행 시간이 걸릴 수 있음**.
- `owner_age_numeric`은 `age`에 날짜·연령대 혼재 시 일부만 유효.
- 확장 모형과 기존 `260301` 결과는 **직접 비교 시** 변동성 정의·회귀자 수가 다름을 명시할 것.
